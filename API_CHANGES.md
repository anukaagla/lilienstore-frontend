# API ცვლილებები — ფრონტისთვის

> **მთავარი წესი:** არცერთი ველი არ წაშლილა. ყველა ცვლილება დამატებითია — ანუ ძველი კოდი არ ჩავარდება.
> მაგრამ **`price` და `min_price`-ის მნიშვნელობა ახლა ვალუტაზეა დამოკიდებული**, ამიტომ ფასის ჩვენება უნდა გადაკეთდეს.

---

## 0. ვალუტის ლოგიკა (ეხება ყველა ფასს)

ბექენდი თავად წყვეტს ვალუტას ვიზიტორის ქვეყნის მიხედვით (Cloudflare `CF-IPCountry`):

| ვიზიტორი | ვალუტა |
|---|---|
| საქართველო | `GEL` |
| ნებისმიერი სხვა ქვეყანა | `USD` |
| ვერ დადგინდა | `GEL` |

**ფრონტს არაფრის გაგზავნა არ სჭირდება** — არც ჰედერის, არც პარამეტრის. ვალუტა არ ირჩევა, გადამრთველი არ არსებობს.

⚠️ **სავალდებულო:** API-ს გამოძახება უნდა ხდებოდეს Cloudflare-ზე გამავალი დომენით (`https://api.lilienstore.com`), **არა** `*.onrender.com`-ით. პირდაპირ onrender.com-ზე მიმართვისას ვალუტა ყოველთვის `GEL` იქნება.

### რა უნდა შეიცვალოს ფრონტზე

```js
// ❌ ასე აღარ
<span>{product.min_price} ₾</span>

// ✅ ასე
const symbol = { GEL: "₾", USD: "$" }[product.currency];
<span>{product.min_price} {symbol}</span>
```

`currency` ველი ახლა ყველა ფასის შემცველ პასუხშია.

---

## 1. 🆕 `GET /api/sections/`

ახალი ენდფოინთი. მთავარი გვერდის სექციები — სათაური + მიბმული კატეგორია + პროდუქტები.

```json
[
  {
    "id": 1,
    "title": { "KA": "ახალი კოლექცია", "EN": "New Collection" },
    "category": {
      "slug": "skirt",
      "name": { "KA": "კაბა", "EN": "Skirt" }
    },
    "products": [ /* იგივე ფორმატი, რაც /api/products/ */ ],
    "updated_at": "2026-08-03T12:00:00Z"
  }
]
```

- მაქსიმუმ **2 სექცია** ბრუნდება, ადმინში დაყენებული რიგით
- `products` — უახლესი პირველი, რაოდენობა ადმინიდან იმართება
- **ღილაკის ბმული:** `category.slug` → `/shop?category=<slug>`
- ავტორიზაცია არ სჭირდება

---

## 2. `GET /api/brand/` — 2 ახალი ველი

```diff
  "logo": "https://.../logo.webp",
  "logo_url": "https://.../logo.webp",
+ "contrast_logo": "https://.../contrast-logo.webp",
+ "contrast_logo_url": "https://.../contrast-logo.webp",
```

`contrast_logo` და `contrast_logo_url` ყოველთვის ერთი და იგივეა. თუ ატვირთული არაა — `null`.

დანარჩენი ველები უცვლელია.

---

## 3. `GET /api/products/` და `GET /api/products/<slug>/`

### ახალი ველი

```diff
  "min_price": 100.0,
+ "currency": "GEL",
```

### შეცვლილი მნიშვნელობა

`min_price` ახლა **მოქმედი ვალუტის** ფასია:

| ვიზიტორი | `min_price` | `currency` |
|---|---|---|
| 🇬🇪 | `100.0` | `"GEL"` |
| 🇺🇸 | `35.0` | `"USD"` |

### ⚠️ პროდუქტები შეიძლება არ დაბრუნდეს

თუ პროდუქტს დოლარის ფასი არ აქვს მითითებული, **უცხოელ ვიზიტორს საერთოდ არ უჩანს**:

- `/api/products/` — სიაში არ მოხვდება
- `/api/products/<slug>/` — **`404 Not Found`**

ფრონტმა 404 ლამაზად უნდა დაამუშაოს (მაგ. "პროდუქტი ვერ მოიძებნა"), რადგან ეს ახლა ნორმალური სცენარია და არა შეცდომა.

### ვარიანტები (`/api/products/<slug>/` → `variants[]`)

```diff
  {
    "id": 2,
    "size": "M",
    "color": "Black",
    "hex_color": "#000000",
    "price": "100.00",
+   "currency": "GEL",
    "stock_qty": 1,
    "allow_order": false
  }
```

- `price` — მოქმედი ვალუტის ფასი (🇬🇪 `"100.00"` / 🇺🇸 `"35.00"`)
- **`price_usd` არ ბრუნდება** — განზრახ, რომ გადართვა შეუძლებელი იყოს
- უცხოელ ვიზიტორს **დოლარის ფასის არმქონე ვარიანტები არ უჩანს** — ანუ `variants` მასივი შეიძლება უფრო მოკლე იყოს

### 📌 ტიპების შენიშვნა

`min_price` არის **რიცხვი** (`100.0`), `variant.price` კი **სტრიქონი** (`"100.00"`). ეს ასე იყო ადრეც, არ შეცვლილა — უბრალოდ გასათვალისწინებელია.

---

## 4. `GET /api/cart/`

```diff
  {
    "id": 5,
    "subtotal": 540.00,
    "shipping_price": 30.00,
    "estimated_total": 570.00,
+   "currency": "GEL",
    "items": [
      {
        "unit_price": 270.00,
        "line_total": 540.00,
+       "currency": "GEL",
        ...
      }
    ]
  }
```

`currency` ორივე დონეზეა (კალათა და თითო item). ყველა თანხა მოქმედ ვალუტაშია, `shipping_price`-ის ჩათვლით.

---

## 5. `POST /api/orders/checkout/` და `GET /api/orders/<id>/`

### ახალი ველი

```diff
  {
    "id": 12,
    "status": "pending",
+   "currency": "USD",
    "subtotal": 198.00,
    "shipping_price": 11.00,
    "total": 209.00,
    ...
  }
```

შეკვეთა **ინახება** იმ ვალუტაში, რომელშიც განთავსდა. ანუ ძველი შეკვეთის ისტორიაში `currency` ყოველთვის სწორია, მიუხედავად იმისა, სად იმყოფება მომხმარებელი ახლა.

`items[].unit_price` — შეკვეთის ვალუტაშია.

### 🆕 ახალი 400 შეცდომა

თუ კალათაში მოხვდა ვარიანტი, რომელსაც დოლარის ფასი არ აქვს, უცხოელის checkout უარიყოფა:

```json
{
  "non_field_errors": [
    "Variant 7 has no USD price and cannot be ordered. Remove it from your cart."
  ]
}
```

ფრონტმა ეს მომხმარებელს გასაგებად უნდა აჩვენოს (მაგ. "ეს პროდუქტი ამჟამად მიუწვდომელია — წაშალეთ კალათიდან").

---

## 6. `GET /api/shipping/settings/`

```diff
  {
    "is_active": true,
    "tbilisi_price": "5.00",
    "georgia_other_price": "12.00",
    "international_price": "0.00",
+   "currency": "GEL",
    "updated_at": "..."
  }
```

ველების სახელები იგივეა, მაგრამ **მნიშვნელობები მოქმედ ვალუტაშია**. ანუ უცხოელი ვიზიტორი დოლარის ტარიფებს დაინახავს იმავე ველებში.

---

## 7. ⚠️ `429 Too Many Requests` — ეხება ყველა ენდფოინთს

დაემატა rate limiting:

| მომხმარებელი | ლიმიტი |
|---|---|
| ავტორიზაციის გარეშე | 120 მოთხოვნა / წუთი |
| ავტორიზებული | 300 მოთხოვნა / წუთი |

```json
{ "detail": "Request was throttled. Expected available in 41 seconds." }
```

პასუხს თან ახლავს `Retry-After` ჰედერი.

**რას ნიშნავს ფრონტისთვის:** თუ სადმე ციკლში ან ხშირი polling-ით იძახებთ API-ს, გადახედეთ. ჩვეულებრივი მოხმარებისთვის ლიმიტი მაღალია და ხელს არ შეგიშლით, მაგრამ 429-ის დამუშავება (მაგ. თავაზიანი შეტყობინება) ღირს.

---

## შეჯამება — რა უნდა გაკეთდეს

| # | სამუშაო | პრიორიტეტი |
|---|---|---|
| 1 | API base URL → `https://api.lilienstore.com` (არა onrender.com) | 🔴 კრიტიკული |
| 2 | ფასის ჩვენება `currency` ველიდან, ჩაწერილი `₾`-ის ნაცვლად | 🔴 კრიტიკული |
| 3 | `/api/products/<slug>/` → 404-ის დამუშავება | 🟠 |
| 4 | checkout-ის ახალი 400 შეცდომის ჩვენება | 🟠 |
| 5 | `GET /api/sections/` ინტეგრაცია მთავარ გვერდზე | 🟡 ახალი ფუნქცია |
| 6 | `contrast_logo`-ს გამოყენება | 🟡 ახალი ფუნქცია |
| 7 | 429-ის დამუშავება | 🟡 |
