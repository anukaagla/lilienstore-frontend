# პასუხი — `BACKEND_REQUESTS.md`

**შეჯამება: ხუთივე პუნქტი მიღებულია. 1–4 გაკეთებულია, მე-5-ზე პასუხები ქვემოთაა.**

თქვენი მთავარი შენიშვნა სწორია და მნიშვნელოვანი. `API_CHANGES.md`-ში დაწერილი
„ფრონტს არაფრის გაგზავნა არ სჭირდება" **SSR-ს არ ითვალისწინებდა** — ჩემი შეცდომაა.
სერვერიდან გამოძახებისას Cloudflare-ს მართლაც თქვენი სერვერის IP დახვდებოდა, და
`/api/orders/checkout/`-ზე შეკვეთა არასწორ ვალუტაში ჩაიწერებოდა. ეს ფულადი ბაგია და
სამართლიანად მონიშნეთ ბლოკერად.

---

## ✅ 1. `X-Visitor-Country` — გაკეთებულია

ზუსტად თქვენი შემოთავაზებული სქემით.

### გამოსაგზავნი ჰედერები

```http
X-Internal-Token: <საერთო საიდუმლო>
X-Visitor-Country: GE          # ISO 3166-1 alpha-2
X-Visitor-IP: 203.0.113.42
```

### ბექენდის ლოგიკა

```
X-Internal-Token სწორია  →  X-Visitor-Country გამოიყენება
                         →  სხვა შემთხვევაში CF-IPCountry
ვერ დადგინდა / XX / T1   →  GEL
```

### უსაფრთხოება — ზუსტად როგორც ითხოვეთ

`X-Visitor-*` ჰედერები **სრულად იგნორირდება**, თუ:
- ტოკენი არ მოვიდა
- ტოკენი არასწორია
- ტოკენი სერვერზე **საერთოდ არაა დაყენებული** (fail-closed)

შედარება timing-safe-ია (`hmac.compare_digest`). ანუ `curl -H "X-Visitor-Country: US"`
ტოკენის გარეშე არაფერს ცვლის — დოკის დანაპირები („გადამრთველი არ არსებობს") ძალაშია.

### 🔑 ტოკენი

`INTERNAL_PROXY_TOKEN` — ბექენდზე დაყენებულია env ცვლადად. თქვენც იმავე მნიშვნელობა
დაგჭირდებათ. **მხოლოდ სერვერულ env-ში** — არასდროს `NEXT_PUBLIC_*`-ში, თორემ
ბრაუზერში მოხვდება და მთელი დაცვა უაზრო ხდება.

ტოკენი ცალკე მოგაწვდით — ჩატში/რეპოში არ იწერება.

### Vary

ვალუტაზე დამოკიდებულ პასუხებს ავტომატურად ემატება:

```http
Vary: X-Visitor-Country, CF-IPCountry
```

ანუ თუ სადმე HTTP-ქეშს გამოიყენებთ, ერთი ვიზიტორის ლარიანი პასუხი მეორეს ვერ მოხვდება.

---

## ✅ 2. Rate limit — `X-Visitor-IP`-ზე — გაკეთებულია

როცა ტოკენი სწორია, throttle-ის გასაღები `X-Visitor-IP`-ია, არა შემომავალი IP.
ანუ 120/წთ ახლა **თითო ვიზიტორზეა**, არა მთელ საიტზე.

პრიორიტეტი:

```
X-Visitor-IP (ტოკენით)  →  CF-Connecting-IP  →  X-Forwarded-For (ბოლო)  →  REMOTE_ADDR
```

ტესტით დადასტურებული: ერთი ვიზიტორის ლიმიტის ამოწურვა დანარჩენებს **არ** ბლოკავს.

> სხვათა შორის, თქვენმა შენიშვნამ ერთი უფრო ადრეული ბაგიც გამოააშკარავა: Cloudflare-ის
> გარეშე throttle `REMOTE_ADDR`-ს იყენებდა, რაც hosting-proxy-ს მისამართია და ყველასთვის
> ერთი. ისიც გასწორდა.

---

## ✅ 3. Checkout 400 — `code` + `variant_ids` — გაკეთებულია

ახალი პასუხი:

```json
{
  "code": "variant_currency_unavailable",
  "currency": "USD",
  "variant_ids": [7, 9],
  "non_field_errors": ["Variant 7, 9 has no USD price and cannot be ordered."]
}
```

- `code` — **სტაბილურია**, ტექსტის შეცვლაზე არ იცვლება
- `variant_ids` — **ნამდვილი რიცხვები**, არა სტრიქონები
- `currency` — ბონუსად, რომ შეტყობინებაში ვალუტა შეძლოთ
- **ყველა** პრობლემური ვარიანტი ერთდროულად ბრუნდება, არა თითო-თითო

⚠️ ტექნიკური დეტალი: DRF ნაგულისხმევად ყველა შეცდომის მნიშვნელობას `force_str`-ით
გადაატარებს, ანუ `[7]` თქვენამდე `["7"]`-ად მოვიდოდა. ამიტომ ცალკე exception handler
დაემატა, რომ `variant_ids` მართლა `int`-ები იყოს. ტესტით დაფარულია.

---

## ✅ 4. `Access-Control-Expose-Headers: Retry-After` — გაკეთებულია

```python
CORS_EXPOSE_HEADERS = ["Retry-After"]
```

429-ზე `Retry-After` ახლა ბრაუზერის JS-იდანაც იკითხება.

---

## 📋 5. პასუხები დასადასტურებელ წვრილმანებზე

ყველა ცოცხალ სერვერზე შემოწმებული, არა კოდის კითხვით.

### 5.1 `/api/brand/` და `/api/categories/` — ფასს **არ შეიცავს** ✅

გადავამოწმე — `price`, `min_price`, `currency` არცერთში არაა.
**დაქეშეთ თავისუფლად.**

⚠️ ერთადერთი დათქმა: `/api/brand/` შეიცავს `hero_category`-ს და `binded_*` ველებს — ესეც
ფასის გარეშეა, ანუ პრობლემა არაა.

### 5.2 `/api/sections/` → `products[]`

- **ფორმატი:** ზუსტად იგივე, რაც `/api/products/`. იგივე serializer-ია.
- **`currency`:** დიახ, თითო პროდუქტში შედის.
- **ფილტრაცია:** დიახ, USD ფასის არმქონე პროდუქტები **გაფილტრულია** უცხოელისთვის.
- **თუ ყველა გაიფილტრა:** სექცია **მაინც ბრუნდება**, `"products": []`-ით.

ცოცხალი შემოწმება (სექცია მიბმულია კატეგორიაზე, სადაც მხოლოდ GEL-პროდუქტია):

```
CF-IPCountry: GE  →  სექციები=1  products=1  currency=GEL
CF-IPCountry: US  →  სექციები=1  products=0  ← ცარიელი მასივი
```

**ფრონტისთვის:** ცარიელი `products[]` ნორმალური სცენარია. ალბათ ჯობია სექცია საერთოდ
არ დახატოთ, თუ `products.length === 0`. თუ გირჩევნიათ, რომ ბექენდმა ასეთი სექცია
საერთოდ არ დააბრუნოს — მითხარით, ეს ერთი ხაზია.

### 5.3 `/api/instagram/embeds/` — **ექვემდებარება** rate limit-ს ✅

ნაგულისხმევი ლიმიტი ყველა ენდფოინთზე ვრცელდება, გამონაკლისების გარეშე.
რადგან ამას ბრაუზერი პირდაპირ იძახებს, ლიმიტი ისედაც თითო ვიზიტორზე გამოვა
(`CF-Connecting-IP`-ით) — ტოკენი აქ არ გჭირდებათ.

---

## რა უნდა გააკეთოთ თქვენს მხარეს

| # | სამუშაო |
|---|---|
| 1 | `INTERNAL_PROXY_TOKEN` სერვერულ env-ში (არა `NEXT_PUBLIC_*`) |
| 2 | SSR/proxy მოთხოვნებს დაამატეთ 3 ჰედერი: `X-Internal-Token`, `X-Visitor-Country`, `X-Visitor-IP` |
| 3 | checkout-ის შეცდომაზე `code === "variant_currency_unavailable"` და `variant_ids` |
| 4 | `/api/brand/` და `/api/categories/` დააქეშეთ 5 წთ |

ჰედერების წყარო თქვენს Cloudflare-ზე: `CF-IPCountry` და `CF-Connecting-IP`
შემომავალი მოთხოვნიდან.

### შემოწმება

ტოკენით — ვალუტა ჰედერს უნდა დაემორჩილოს:
```bash
curl -s -H "X-Internal-Token: <token>" -H "X-Visitor-Country: US" \
  https://api.lilienstore.com/api/products/ | head -c 200
```

ტოკენის გარეშე — იგივე ჰედერი უნდა იგნორირდეს (`GEL` დარჩეს):
```bash
curl -s -H "X-Visitor-Country: US" https://api.lilienstore.com/api/products/ | head -c 200
```

---

## შენიშვნა თავსებადობაზე

ცვლილებები **უკუთავსებადია**. თუ ჰედერებს ჯერ არ გააგზავნით, ქცევა ზუსტად ისეთი
დარჩება, როგორიც ახლაა — `CF-IPCountry`-ზე დაყრდნობით. ანუ deploy-ის თანმიმდევრობა
თავისუფალია, ერთდროულად აწყობა არ გჭირდებათ.

---

კითხვები თუ გაგიჩნდებათ, ამავე ფოლდერში `.md` ფაილი დადეთ — ვაკონტროლებ.
